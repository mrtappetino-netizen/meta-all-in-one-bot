# ==============
# CONFIGURAZIONE META / OPENAI BOT
# ==============

# Token di verifica usato durante la registrazione webhook su Meta (Messenger, IG, WhatsApp)
VERIFY_TOKEN=metaverify_123

# App Secret della tua app Meta (vai su developers.facebook.com → Impostazioni → Base → App secret → Mostra)
APP_SECRET=INSERISCI_QUI_APP_SECRET

# Imposta a false solo per testare se la firma HMAC blocca i webhook
VERIFY_SIGNATURE=true

# ==============
# ACCESS TOKEN & ID META
# ==============

# Token della Pagina Facebook (usato sia per Messenger che per Instagram DM)
PAGE_ACCESS_TOKEN=INSERISCI_QUI_PAGE_ACCESS_TOKEN

# Token della WhatsApp Cloud API (lo trovi in developers.facebook.com → WhatsApp → Configuration)
WHATSAPP_TOKEN=INSERISCI_QUI_WHATSAPP_TOKEN

# ID del numero WhatsApp Cloud (es. 123456789012345)
PHONE_NUMBER_ID=INSERISCI_QUI_PHONE_NUMBER_ID

# ==============
# OPENAI CONFIG
# ==============

# Chiave API di OpenAI (inizia con sk-...)
OPENAI_API_KEY=sk-INSERISCI_CHIAVE_OPENAI

# Modello da usare: gpt-4o-mini (più veloce ed economico)
OPENAI_MODEL=gpt-4o-mini

# ==============
# SERVER
# ==============

# Porta locale per sviluppo (Render la ignora e usa automaticamente la sua)
PORT=3000
